node_modules/
__pycache__/
.env*

# Intermediate build artifact — build.sh writes it, then copies it to site/data.json.
# Committing both duplicates ~6MB per refresh. site/data.json is the published one.
data/products.json
